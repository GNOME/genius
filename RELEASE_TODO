Things not to forget for release
1) Run geniustests.pl
2) run ./update-xml-to-txt-html.sh in help
3) check the .spec file?  Can't test it though
4) ... what else?

Tagging stuff:
The name of the tag must be the release
The last commit must be signed git commit --gpg-sign
git evtag sign 1.0.28
git push origin 1.0.28
