# Misc Constants/functions for Dr. Genius

# Kind of stupid function, but could be useful somewhere
SetHelp ("Identity", "basic", "Identity function, returns its argument")
function Identity(x) = x
protect ("Identity")

#make something a string
SetHelp("string","basic","Make a string");
function string(s) = s + ""
protect("string")

SetHelp("Compose","basic","Compose two functions")
function Compose(f,g) = `(x)=f(g(x))
protect("Compose")
