
    <sect1 id="genius-gel-sums-products">
      <title>Sums and products</title>
      <para>
Syntax:
<programlisting><![CDATA[sum <identifier> = <from> to <to> do <body>
sum <identifier> = <from> to <to> by <increment> do <body>
sum <identifier> in <matrix> do <body>
prod <identifier> = <from> to <to> do <body>
prod <identifier> = <from> to <to> by <increment> do <body>
prod <identifier> in <matrix> do <body>]]></programlisting>

If you substitute <command>for</command> with <command>sum</command> or <command>prod</command>, then you will get a sum or a product instead of a <command>for</command> loop. Instead of returning the last value, these will return the sum or the product of the values respectively.
      </para>
      <para>
If no body is executed (for example <command>sum i=1 to 0 do ...</command>) then <command>sum</command> returns 0 and <command>prod</command> returns 1 as is the standard convention.
      </para>
    </sect1>
